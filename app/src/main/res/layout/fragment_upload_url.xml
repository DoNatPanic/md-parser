<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/FragmentBgStyle"
    tools:context=".ui.upload.fragment.UploadUrlFragment">

    <LinearLayout
        android:id="@+id/layout_file_select"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/content_margin_top"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <EditText
            android:id="@+id/url_input"
            android:layout_width="0dp"
            android:layout_weight="1"
            style="@style/RegularTextStyle"
            android:hint="@string/input_url_hint_str"
            android:imeOptions="actionDone"
            android:inputType="textUri"
            android:layout_marginEnd="@dimen/link_text_margin_end"
            android:singleLine="true" />

        <ImageView
            android:id="@+id/go_link_icon"
            android:layout_width="@dimen/icon_size"
            android:layout_height="@dimen/icon_size"
            android:clickable="true"
            android:src="@drawable/outline_arrow_circle" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>